@model List<NewsPostViewModel>

@inject IUser userService;
@{
    var name = User.Identity!.Name;

    var user = userService.Get(name);

    var color = UserMethods.GetColor(user.SocNetItems.X, user.SocNetItems.Y);
}

<div class="news">

    <partial name="Partial/News/AddNews" model="@(new ComModel())"/>

    <partial name="Partial/News/NewsPosts" model="@Model"/>
</div>

<partial name="Partial/News/NewsRghtSiteBar" />
